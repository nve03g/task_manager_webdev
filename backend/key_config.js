const JWT_SECRET_KEY = 'UTe3aQrD.wUpmg4@qANvyw'; // used to sign and verify the JWT token
// IMPORTANT: sla deze op ergens buiten de code (en buiten de git), niet hard-coden!
// (not safe to do it this way, but for the sake of the project I saved the secret key like this)

module.exports = {
    JWT_SECRET_KEY,
};